import React from "react";
import { Layout } from "../components/layout/Layout";

const Ricettario = () => {
  return (
    <Layout>
      <div>ricettario</div>
    </Layout>
  );
};

export default Ricettario;
